module # (parameter CENTER = 0) LED_2x1 (clk, reset, top, bottom, out);
	input logic clk, reset;
	input logic top, bottom;
	output logic out;
	
	enum { off, on } ps, ns;
	
	always_comb begin
		case(ps)
			off:		out = 2'b00;
			on:		out = 2'b11;
			
			
	end
	
endmodule
